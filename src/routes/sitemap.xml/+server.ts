export const prerender = true; const pages=['/','/projects','/resume','/contact']; export function GET(){ const origin='https://your-domain.example'; const body=`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${pages.map(p=>`<url><loc>${origin}${p}</loc></url>`).join('')}</urlset>`; return new Response(body,{headers:{'Content-Type':'application/xml'}});}